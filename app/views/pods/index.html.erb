<%= search_form_for @q do |f| %>
  <%= f.search_field :name_cont,placeholder: "Search Pods..." %>
  <%= f.submit %>
<% end %>
<ol>
<% @pods.each do |p|%>
  <li><%= p.name %><%= link_to " Apply" ,new_pod_request_path(id: p.id)%></li>
<%end%>
</ol>
